#!/usr/bin/env bash

# List tags defined for given Docker image

if [[ "$1" == '' ]]; then
  echo 'Argument missing'
  exit 1
fi

http get "https://registry.hub.docker.com/v2/repositories/${1}/tags" page_size==1024 | jq -r '.results[].name' | less --quit-if-one-screen
