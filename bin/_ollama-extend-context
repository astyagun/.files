#compdef ollama-extend-context

local -a models
models=(
  $(
    ollama list |
    tail -n +2 |
    grep -vE '\S+:\S+k' |
    ruby -ane 'puts $F.values_at(0, 2, 3).join(" ")' |
    sed -e 's/:/\\:/' -e "s/ /:/" |
    sed -E 's/ ([A-Z]+)$/\1/'
  )
)
_describe 'local models' models
